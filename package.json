{
  "name": "kepixel-tracking-scanner",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/scan.js && node --check src/scanner-core.js && node --check src/browser-scan.js && node --check src/signatures.js && node --check src/network.js && node --check src/detection.js && node --check src/quality.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@sparticuz/chromium": "^131.0.0",
    "cheerio": "^1.0.0",
    "puppeteer-core": "^23.11.1"
  }
}
